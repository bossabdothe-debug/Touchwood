"use client"; import { useEffect, useState } from "react"; import { useParams, useRouter } from "next/navigation"; type AdminGuardProps = { children: React.ReactNode; }; export default function AdminGuard({ children, }: AdminGuardProps) { const router = useRouter(); const params = useParams(); const locale = params?.locale === "en" ? "en" : "ar"; const [checking, setChecking] = useState(true); const [allowed, setAllowed] = useState(false); useEffect(() => { const token = localStorage.getItem("token"); const storedUser = localStorage.getItem("user"); if (!token || !storedUser) { router.replace(`/${locale}/login`); return; } try { const user = JSON.parse(storedUser); if (user.role !== "admin") { setAllowed(false); setChecking(false); return; } setAllowed(true); setChecking(false); } catch { localStorage.removeItem("token"); localStorage.removeItem("user"); router.replace(`/${locale}/login`); } }, [locale, router]); if (checking) { return ( <div style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", background: "#f8faf8", }} > <div style={{ width: "42px", height: "42px", border: "3px solid #eaf6ec", borderTopColor: "#238a37", borderRadius: "50%", animation: "admin-guard-spin 0.8s linear infinite", }} /> <style jsx>{` @keyframes admin-guard-spin { to { transform: rotate(360deg); } } `}</style> </div> ); } if (!allowed) { return ( <main dir={locale === "ar" ? "rtl" : "ltr"} style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", padding: "24px", background: "#f8faf8", }} > <section style={{ width: "100%", maxWidth: "520px", padding: "48px 32px", textAlign: "center", background: "#ffffff", border: "1px solid #e5ebe6", borderRadius: "24px", boxShadow: "0 20px 60px rgba(20, 60, 30, 0.08)", }} > <div style={{ width: "72px", height: "72px", margin: "0 auto 24px", display: "flex", alignItems: "center", justifyContent: "center", borderRadius: "50%", background: "#fef3f2", color: "#b42318", fontSize: "32px", fontWeight: 700, }} > ! </div> <h1 style={{ margin: "0 0 12px", color: "#151515", fontSize: "28px", fontWeight: 700, }} > {locale === "ar" ? "الوصول غير مسموح" : "Access Denied"} </h1> <p style={{ margin: "0", color: "#6f7470", fontSize: "15px", lineHeight: 1.8, }} > {locale === "ar" ? "ليس لديك صلاحية للوصول إلى لوحة التحكم." : "You do not have permission to access the admin dashboard."} </p> </section> </main> ); } return <>{children}</>; }